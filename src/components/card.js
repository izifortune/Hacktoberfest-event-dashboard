import styled from "styled-components";
import { bodyColor } from "../vars";

export const Card = styled.div`
  padding: 1.5rem 1.5rem;
  position: relative;
  background-color: #1d2c4e;
  box-shadow: 0px 50px 100px rgba(0, 0, 0, 0.2);
  color: ${bodyColor};
  padding: 1.25rem;
`;
